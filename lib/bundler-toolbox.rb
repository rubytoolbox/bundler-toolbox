# frozen_string_literal: true

# This file exists to allow to use `gem "bundler-toolbox"` without an explicit require
# as well as doing `require "bundler-toolbox"` (the gem name) instead of, rubygems naming
# patterns guidelines `require "bundler/toolbox"`
require "bundler/toolbox"
